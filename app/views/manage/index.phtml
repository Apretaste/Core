<div class="row  border-bottom white-bg dashboard-header">
	<div class="col-sm-12">
		<div class="row text-left">
			<div class="col-xs-4">
				<div class=" m-l-md">
					<span class="h4 font-bold m-t block"><?php echo $totalUsers;?></span> <small class="text-muted m-b block">Total users</small>
				</div>
			</div>
			<div class="col-xs-4">
				<span class="h4 font-bold m-t block">$ <?php echo number_format($sumCredit, 2);?></span> <small class="text-muted m-b block">Total of credit</small>
			</div>
			<div class="col-xs-4">
				<span class="h4 font-bold m-t block"><?php echo $utilization; ?></span> <small class="text-muted m-b block">Utilization</small>
			</div>
		</div>
	</div>
</div>
<br/>
<div class="row">
	<div class="col-xs-4">
		<div class="ibox float-e-margins">
			 <div class="ibox-title">
				<h5><a href="/manage/remarketing">Tasks status</a></h5>
			</div>
			<div class="ibox-content">
				<table class="table table-condensed fullscreen">
					<thead>
						<tr>
							<th>Task</th>
							<th>Days behind</th>
							<th>Delay (sec)</th>
						</tr>
					</thead>
					<tbody>
						<?php foreach ($tasks as $task) { ?>
						<tr class="<?= $task->days > $task->frequency ? "danger" : "success"?>">
							<td><?= $task->task ?></td>
							<td class="text-center"><?= $task->days ?></td>
							<td class="text-center"><?= $task->delay ?></td>
						</tr>
						<?php } ?>
					</tbody>
				</table>
			</div>
		</div>

		<div class="ibox float-e-margins">
			 <div class="ibox-title">
				<h5><a href="/manage/remarketing">Customer Support</a></h5>
			</div>
			<div class="ibox-content">
				<table class="fullscreen">
					<tr>
						<td>New Tickets</td>
						<td class="text-center"><?=$supportNewCount?></td>
					</tr>
					<tr>
						<td>Pending Tickets</td>
						<td class="text-center"><?=$supportPendingCount?></td>
					</tr>
				</table>
			</div>
		</div>
	</div>

	<div class="col-xs-4">
		<div class="ibox float-e-margins">
			<div class="ibox-title">
				<h5><a href="/manage/dropped">Delivery status</a> <small>(Last 7 days)</small></h5>
				<?php if ($deliveryFailurePercentage > 2): ?>
				<div class="ibox-tools">
					 <span class="label label-danger pull-right">ALERT !!!</span>
				</div>
				<?php endif; ?>
			</div>
			<div class="ibox-content">
				<table class="fullscreen">
					<tr>
						<td width="50%">delivered: <?= number_format($delivery['delivered']); ?></td>
						<td>hardfail: <?= isset($delivery['hardfail']) ? $delivery['hardfail'] : 0; ?> (<?= $deliveryFailurePercentage; ?>%)</td>
					</tr>
					<tr>
						<td width="50%">hard-bounce: <?= isset($delivery['hard-bounce']) ? $delivery['hard-bounce'] : 0; ?></td>
						<td>soft-bounce: <?= isset($delivery['soft-bounce']) ? $delivery['soft-bounce'] : 0; ?></td>
					</tr>
					<tr>
						<td width="50%">spam: <?= isset($delivery['span']) ? $delivery['span'] : 0; ?></td>
						<td>no-reply: <?= isset($delivery['no-reply']) ? $delivery['no-reply'] : 0; ?></td>
					</tr>
					<tr>
						<td width="50%">loop: <?= isset($delivery['loop']) ? $delivery['loop'] : 0; ?></td>
						<td>temporal: <?= isset($delivery['temporal']) ? $delivery['temporal'] : 0; ?></td>
					</tr>
					<tr>
						<td width="50%">failure: <?= isset($delivery['failure']) ? $delivery['failure'] : 0; ?></td>
						<td>unknown: <?= isset($delivery['unknown']) ? $delivery['unknown'] : 0; ?></td>
					</tr>
				</table>
			</div>
		</div>
	</div>
	<div class="col-xs-4">
		<div class="ibox float-e-margins">
			<div class="ibox-title">
				<h5>Promoters <small>(unpaid)</small></h5>
			</div>
			<div class="ibox-content">
				<table class="table table-condensed fullscreen">
					<thead>
						<tr>
							<th>Promoter</th>
							<th class="text-center">Unpaid</th>
							<th class="text-center">Last</th>
						</tr>
					</thead>
					<tbody>
						<?php foreach ($promoters as $promoter): ?>
						<?php $daysElapsed = (time() - strtotime($promoter->last_usage))/60/60/24; ?>
						<?php if( ! $promoter->total) $promoter->total = 0; ?>
						<tr <?php if($promoter->active == 0) echo 'class="danger"'; else if($daysElapsed > 31) echo 'class="warning"'; ?>>
							<td><?= $promoter->email ?></td>
							<td class="text-center"><?= $promoter->total ?></td>
							<td class="text-center"><?= date("m/d/y", strtotime($promoter->last_usage)) ?></td>
						</tr>
						<?php endforeach; ?>
					</tbody>
				</table>
			</div>
		</div>
	</div>
</div>
