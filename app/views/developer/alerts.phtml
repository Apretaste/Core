<div class="row">
    <div class="col-xs-12">
        <form action="/developer/alerts" method="POST">
            <div class="input-group">
                <input required type="email" class="form-control" name="filter" value="<?=$query?>" placeholder="type text here...">
                <span class="input-group-btn">
					<button type="submit" class="btn btn-success">Search</button>
				</span>
            </div>
        </form>
        <br/>
    </div>

        <form action="/developer/alerts" method="POST">
            <input type="hidden" name="filter" id="filter" class="form-control" value="<?=$query?>">
            <table id="servicesTable" class="table">
                <thead>
                <tr>
                    <th width="50" class="text-center"><input type="checkbox" onclick="$('.chkFix').click();"></th>
                    <th>Date</th>
                    <th class="text-center">Text</th>
                </tr>
                </thead>
                <tbody>
                <?php if ($no_results): ?>
                    <div role="alert" class="alert alert-danger"><span>No alerts<?php if (!empty($query)): ?> for <b><?=$query?></b><?php endif; ?>.</span></div>
                <?php endif; ?>

                <?php foreach ($alerts as $alert) {
                    ?>
                    <tr>

                        <td class="text-center" valign="middle"><input type="checkbox" name="fixed[]" class="chkFix" value="<?=$alert->id?>"></td>
                        <td valign="middle"><?= date("Y/m/d H:i:s", strtotime($alert->created)) ?></td>
                        <td class="text-left"><?=$alert->text?></td>
                    </tr>
                <?php } ?>
                </tbody>
            </table>
            <p align="right"> <input type="submit" class="btn btn-default" value="Fix all checked" onclick="return confirm('Are you sure?');"></p>
        </form>
	</div>
</div>
